# AI 使用说明

按作业要求写三件事：用了什么、**AI 判断错在哪、我是怎么发现的**、哪些判断不交给 AI。

作业里写着「我们全团队都用 Claude Code 开发，用 AI 不扣分，被 AI 骗了没发现才扣分」，
所以第二节写得最细——那一节的每一条都是真实发生过的误判，以及让它露馅的那个实验。

## 1. 用了什么，怎么拆任务

| 项 | 内容 |
|---|---|
| 工具 | **Claude Code**（命令行），全程使用 |
| 用法 | 它读代码、跑命令、改文件、跑测试；**git 全部由我手动敲**（`add` / `commit` / `push` 都不让它执行） |
| 为什么不用它碰 git | 提交历史本身是评分项，得由我自己对每一次提交负责 |

**怎么拆任务**（这几条是硬性约定，写下来是为了让它别一次写一大片）：

1. **一关做完、验证通过、提交，再开下一关。** 不允许一次性写完所有关卡的代码。
2. **每个缺陷先写会红的测试，再修。** 修复前必须先把测试跑给我看是红的，
   并且红的是**那一条**——作业第二关明确要求这个提交顺序。
3. **每个模块做完就停下**，输出 `git add` / `git commit` 的命令文案，由我手动执行；
   我回复「已提交」之后才继续。
4. **涉及三个以上文件的改动，先出中文实施方案**，我确认后再动手。

**一个真实的 prompt 例子**（我发给它的第一条，节选）：

```
我们现在做Moneki.ai的take-home项目，规则严格遵守：
1. 开发顺序：一关做完，验证通过，通知我执行git commit，再继续下一关。禁止一次性写完所有代码。
2. 每完成一个小功能模块，你必须停下来，告诉我【这部分开发完成，请我手动执行git add / commit / push】，
   并且给我对应的commit备注文案。你不能帮我执行git命令，git操作全部由我在PowerShell手动敲。
3. 不要一次性大规模改动代码，小步迭代。

第一步：先通读项目全部文件，阅读docs里API_CONTRACT.md契约，了解starter的FastAPI代码结构…
```

值得注意的是第 2 条在整份作业里救了我好几次：**因为它必须停下来等我提交，
每次都要先给出「这个模块做了什么、怎么验证的」**，所以它没法用「大概改好了」糊过去。
下面那些被推翻的误判，大多是在写那段说明、准备证据的时候露出来的。

## 2. AI 判断错的地方，以及是怎么发现的

这一节是这份文件的主要部分。**下面每一条都是它先说错、后来被实验推翻的**，
我把让错误露馅的那个动作一并写上——这才是可复用的部分。

### 2.1 「检索全 0 分」的第一次归因是错的

**它的判断**：`/api/retrieve` 对不同问题返回同一组文档，它先说这是
「先取前 `top_k`、再按元信息过滤」的顺序反了（评测说明里确实点过这个坑）。

**怎么发现不对**：我让它先做最简单的核对——四道**毫不相关**的问题返回了**逐字相同**的
五篇文档，而且每一段的 `score` 都恰好是 `0.0000`。顺序问题只会让条数变少，
不会让不同的问题返回同一组结果。它随后才去查打分环节，两分钟内定位到
`tokenize()` 是「按空白切词」，中文整句成了一个词元。**根因和第一版归因完全无关。**

### 2.2 「缓存不一致 / chunk 重复」也是错的方向

**它的判断**：修完分词之后，出现「`tests/test_retriever.py` 单跑是绿的、全量跑 8 条红」。
它先怀疑索引缓存与内存不一致，又说可能是有重复的 `chunk_id` 导致按 id 建字典时互相覆盖。

**怎么发现不对**：这两个假设都能用一行命令否掉——`len(chunks)=95`、去重后仍是 95，没有重复；
重建索引后现象不变。真正的原因是第三个假设：**换测试文件顺序重跑**，
`pytest tests/test_api.py tests/test_retriever.py` 红、反过来绿。
那是 `conftest.py` 把检索替身打在 **`Retriever` 类**上且从不还原，
session 级生效，于是我的新测试全跑在一份写死的假数据上。

**这一条顺带推翻了一个更重要的东西**：交接文档写着「测试全部通过」。
测试确实全绿，但**它把检索整个换成了固定返回**——绿灯和「检索是对的」之间没有任何关系。
我要求把这一条写进 `DEBUG_LOG.md`（D3），而不是当成自己代码的 bug 修掉就算了。

### 2.3 「句子级重排压过了文档级排序」——三次实验都不是它

**它的判断**：`doc` 类 0/16。它认为 `answerer._candidates()` 里文档分数的权重被开平方根，
差异被压缩，于是句子级的关键词重合度反过来主导排序。它给出的修法是把这个指数调大。

**怎么发现不对**：指数从 `0.5` 改成 `1.0`，跑一遍；再改成 `2.0`，再跑一遍——
**两次都是 70.00，一分没动。** 一个假设如果调它的参数完全不影响结果，那它就不是原因。
真正的原因是同一行代码里另外两个东西：`candidates.sort()` 用的是**升序**，
而紧接着的循环是照着这个顺序取前两名的——**它一直在从分数最低的证据开始挑**
（实测：KB-011 里那句加权分 1.5444，第二名只有 0.0855，差 18 倍，却落选了）。

### 2.4 「把倒原文删掉就好」——删了反而更低

**它的判断**：`_answer_doc` 会把检索第一名那篇文档的**全部片段**倒进 `answer`，
所以 1701 字、24 个数字，越过契约上限。它主张直接删掉这段。

**怎么发现不对**：我让它先做对照实验再改结论。删掉之后 **70.00 掉到 69.00**——
`doc` 涨了 2 分，`version` 掉 2 分、`multi_turn` 掉 1 分。
原来那段倒出虽然难看，却**部分有效**：它把事实塞进了 `answer`，正好满足评测
「事实要出现在 `answer` 或 `quote` 里」的检查。

于是改法变了：**先修排序（2.3），再删倒出**。两步分开测，70 → 78 → 84。
如果一次全改，这个「删了更差」的信号会被淹没，我会得出相反的结论。

### 2.5 它写的测试，假设本身是错的

**发生了什么**：做意图闸门时它写了两组测试——「必须拒答的」和「必须照常回答的」。
跑出来三条红，红的都在第二组：「9 月的营业额是多少」「我们员工的平均工资是多少」
「员工迟到多久算一次」。

**怎么发现不对**：查下去发现前两句**本来就该被拒答**，只是该由另外两条既有机制
（「区间外」「越界」）来裁，而不是被新闸门裁。**它把「不被这个闸门拒」和「不被拒答」
混为一谈了。** 这是它写的测试自己的缺陷，不是代码的缺陷。

改法：把测试分成三组（必须拒 / 必须答 / 该拒但该由别的机制裁），
最后那组还多守住一条——加了闸门之后，别的拒答路径既不能被顶掉、也不能替它背锅。

### 2.6 一次无效的测量，差点被当成「修复无效」

**发生了什么**：修完「会话历史没传给规划器」之后跑评测，**分数停在 64.50 一动不动**。
它一度准备按「这个修复没用」往下走。

**怎么发现不对**：它去看了新服务的启动日志——里面是
`[Errno 10048] error while attempting to bind on address ('127.0.0.1', 8000)`：
**端口被上一个进程占着，新服务根本没起来，评测打的是没有新代码的旧服务。**
停掉旧进程、确认日志里有 `Application startup complete` 之后重跑，分数是 70.00（`multi_turn` 满分）。

教训我让它写进了 `DEBUG_LOG.md`：**起服务之后要确认真的绑上了端口，而不是只看命令返回了。**
同一类错误在这份作业里出现过两次（另一个是缓存没重建），所以它现在改完代码会顺手确认
`.cache/index.json` 的时间戳或缓存键有没有跟着变。

### 2.7 把「我的环境有问题」误判成「Vite 有 bug」，还据此写了个多余的补丁

**它的判断**：`web/dist/` 里堆着三次构建的旧产物，`index.html` 只引用其中一个。
它认定是 `vite.config.js` 里的 `emptyOutDir` 没生效，进而断言「Vite 8.3.0 的 `emptyOutDir`
实测不生效（连 CLI 传 `--emptyOutDir` 也不清）」，并为此加了一个 `prebuild` 脚本自己删 `dist`。

**怎么发现不对**：为了确认「删除到底有没有执行」，它写了一个最小实验——用 Node 新建一个
`_probe.txt` 再删掉：

```
创建 _probe.txt: true
删除后 _probe.txt 还在吗: true      ← 删除被静默忽略了
```

**它自己的运行环境静默忽略所有删除操作**——`fs.rmSync` 既不生效也不抛异常。
所以 `emptyOutDir` 和 `prebuild` 都"没生效"是**同一个原因**，跟 Vite 一点关系都没有。

**处理**：把那个基于错误诊断加上的 `prebuild` 脚本**撤回**了——留着一个用错误理由写出来的补丁，
下次别人看到会以为 Vite 真有问题。清理 `dist` 由我在自己的 PowerShell 里执行一次即可
（Vite 的 `emptyOutDir` 在正常环境里是好的）。

**这一条值得单独记下来的原因**：前六条错的都是「对代码的归因」，这一条错的是
**「对环境的归因」**——而环境的问题最容易被当成代码的 bug，因为现象一模一样。
以后遇到「某个标准机制不生效」，先写一个最小实验确认**这个动作本身能不能做**，
再去看那个机制的实现。

### 2.8 一句话里两个"看着像"的坑

- **它判断 `POLICY_WORDS` 漏了词**（问句里的「多久」「几点」没被识别成问规定）。
  读代码发现「多久」「几点」**本来就在那张表里**——真正的问题是后面还有一段兜底路由
  又把结论推翻成取数题。**不读代码只看现象的话，这一条会改错地方。**
- **它以为「外卖订单多久内可以申请退款」里的「卖」是销售动词。**
  做规则时才发现那是「外**卖**」这个词里的字，单字匹配会误判一整个类别的题。

## 3. 哪些判断我自己做的，为什么不交给它

| 判断 | 为什么不交给它 |
|---|---|
| **口径按 v3 还是 v2** | 知识库里有新旧两版手册，且 v3 相对 v2 改的三处**每一处都会改变算出来的数字**（退款计不计入营业额、空金额要不要回填、客单价的分母）。这是业务判断，选错整份报表都是错的。文档里有明确依据（`status: 现行` / `已被取代`），但「以哪一版为准」这个决定由我做，并要求它把理由写进 README。 |
| **退款行的 `qty` 记正数还是负数** | 这决定了销量公式是「减」还是「加」。它第一版写的测试用了负数（按常识猜的），跑出来红了；我要它先去数据里数一遍，实测 94 行退款行 `qty` **全为正**，于是公式按手册字面实现，并加一条测试守住这个数据约定——哪天数据换了符号，测试会先红，而不是让销量静默算反。 |
| **R04 到此为止，不再调参** | 跨语言检索差 13% 进不了前五。它可以继续调权重、试各种系数，但那是**拿公开题当靶子去凑分**：真正的瓶颈是「中文问句与英文正文没有共同词元」，调参治不了。我要它把量化结论、被推翻的假设、以及「量产化的修法是接多语种向量模型」写进 `DEBUG_LOG.md` 与 README 的已知限制——**留 1 分不拿，换一份说得清的结论**。 |
| **那个「通用英文词误桥接」的缺陷先不修** | `cold`（来自别名 Cold Brew Oolong）与供应商名 Tasman **Cold** Chain 相撞，给无关文档注入了噪声词元。修法我认可（按语料频次再筛一道），但它**不影响 R04 的结论**，却动在所有检索的公共路径上、有回归风险。我要求先用 `@pytest.mark.xfail` 标注、理由写进 `reason`，不藏起来，等有余力再动。 |
| **提交粒度与顺序** | 每个缺陷一个提交、每个提交单独看都是绿的。所以像「分词器」「测试替身」「命中标注」这类同一个根因下的三处修改，我要求拆成三个提交而不是一个。作业明说了会看提交历史还原调试过程，「只有一个 finish commit 的仓库直接不看」。 |
| **不加任何写数据库的能力** | 发现的 `run_sql` 能真改数据之后，我要它做的是**两层只读**（意图闸门 + SQLite `mode=ro`），而不是「小心一点」或「加个黑名单」。数据库不能有任何改动是硬要求，得由数据库自己兜住。 |

## 4. 一句话总结

AI 在这份作业里最有用的是**跑实验和读代码**：它能很快地把「某个假设」变成一个可执行、
可证伪的对照实验，也能把 30 个模块的文件读到定位到某一行。

它最容易出错的地方是**归因**——看到现象先给一个听起来合理的解释，
而那个解释往往和真正的原因无关（第 2 节七条全是这类）。
唯一有效的对策是把它说的每一句都变成一个能跑的实验：**能证伪的才留下。**
这也是作业里那句「不要默认任何人说的都是对的，包括前同事的交接文档」的另一种写法——
交接文档说「检索命中率 95%、测试全部通过」，实测下来两条都不成立。
